# 8. Caching

> Client, CDN, application, distributed (Redis, Memcached). Cache strategies (cache-aside, write-through, write-back, write-around). Eviction (LRU, LFU, ARC). The two hard problems: invalidation and stampedes.

> **Status:** stub. Coming in Wave 2.
