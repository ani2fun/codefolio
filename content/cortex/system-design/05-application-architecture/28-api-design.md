# 28. API design — REST, gRPC, GraphQL

> Versioning, pagination, error contracts. When to pick each. Why "REST or gRPC" is the wrong question and "what does the client actually look like?" is the right one.

> **Status:** stub. Coming in Wave 4.
